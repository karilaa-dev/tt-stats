---
import { localeFromRequest } from "@/lib/i18n/locale"
import Layout from "@/src/layouts/base.astro"
import Dashboard from "@/components/pages/me-island"
import { isFakeDataEnabled } from "@/lib/dev/fake-data"
const pathname = Astro.url.pathname.replace(/\/$/, "")
---

<Layout title="My Profile · @ttgrab Stats">
  <Dashboard
    locale={localeFromRequest(Astro.cookies, Astro.request)}
    client:load
    pathname={pathname}
    search={Astro.url.search}
    fakeMode={isFakeDataEnabled()}
  />
</Layout>
