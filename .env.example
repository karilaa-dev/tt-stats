# One administrative PostgreSQL connection for reads, website storage and setup.
# Superusers are supported; the role must be able to manage the website schemas.
DB_URL=postgresql://tt_stats:replace-me@127.0.0.1:5432/ttbot-db
DB_POOL_SIZE=5
# Log all database query stages and timings temporarily while troubleshooting.
# Slow queries and failures are logged even when this is false.
DB_QUERY_DEBUG=false

# Numeric Telegram user ID granted admin access after verified Telegram login.
# Leave unset to keep Operations and individual user lookup locked.
ADMIN_TELEGRAM_ID=

# Optional Telegram-calculated MAU. Supply all three to connect MTProto.
# Obtain API credentials at https://my.telegram.org/apps
# Saved-media previews require the token of the bot that owns the stored files.
# BOT_TOKEN=12345:replace-with-telegram-bot-token
# TELEGRAM_API_ID=123456
# TELEGRAM_API_HASH=replace-with-32-character-api-hash

# Optional video-download inactivity notifications. Configure exactly one.
# A generic webhook receives a JSON POST body.
# VIDEO_INACTIVITY_WEBHOOK_URL=https://example.com/webhooks/tt-stats

# Or publish to an ntfy topic URL. The bearer token is optional.
# VIDEO_INACTIVITY_NTFY_URL=https://ntfy.sh/replace-with-private-topic
# VIDEO_INACTIVITY_NTFY_TOKEN=replace-with-ntfy-access-token

# Development only: render deterministic sample data without PostgreSQL.
# TT_STATS_FAKE_DATA=true

# Development only: comma-separated public origins allowed to reach Vite.
# DEV_ALLOWED_ORIGINS=https://example.test

# Official Telegram OAuth. Keep the client secret on the server.
# Obtain these from your bot's Login Widget settings in @BotFather.
# Keep the default RS256 signing algorithm enabled.
TELEGRAM_OAUTH_CLIENT_ID=
TELEGRAM_OAUTH_CLIENT_SECRET=
# Set consistently at build time and runtime. Register this origin and
# https://tt-stats.karilaa.dev/api/auth/telegram/callback with BotFather.
APP_ORIGIN=https://tt-stats.karilaa.dev

# In-memory token buckets. COUNT is replenished over the policy's window;
# BURST is the maximum immediate allowance. All values must be positive.
# Reads, metadata and media use a 60-second window; CSV and login use 600 seconds.
# RATE_LIMIT_READ_COUNT=120
# RATE_LIMIT_READ_BURST=60
# RATE_LIMIT_METADATA_COUNT=60
# RATE_LIMIT_METADATA_BURST=20
# RATE_LIMIT_MEDIA_COUNT=240
# RATE_LIMIT_MEDIA_BURST=60
# RATE_LIMIT_CSV_COUNT=5
# RATE_LIMIT_CSV_BURST=2
# RATE_LIMIT_LOGIN_COUNT=10
# RATE_LIMIT_LOGIN_BURST=5
