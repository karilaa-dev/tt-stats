# Read-only access to the current tt-bot PostgreSQL database.
DB_URL=postgresql://tt_stats:replace-me@127.0.0.1:5432/ttbot-db
DB_POOL_SIZE=5

# Aggregate statistics stay in memory and refresh on the application server.
STATS_REFRESH_INTERVAL_SECONDS=300
STATS_CACHE_IDLE_MINUTES=30

# Manual Botstat.io verification.
BOT_TOKEN=12345:replace-with-telegram-bot-token
BOTSTAT_ACCESS_KEY=replace-with-botstat-access-key
BOTSTAT_NOTIFY_ID=1234567
BOTSTAT_BASE_URL=https://www.botstat.io

# Development only: render deterministic sample data without PostgreSQL.
# TT_STATS_FAKE_DATA=true

# Development only: comma-separated public origins allowed to reach Vite.
# DEV_ALLOWED_ORIGINS=https://example.test
